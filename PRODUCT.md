# 青梧产品说明

## 为什么做青梧

青梧 fork 自妙言（MiaoYan，MIT 许可）。做它的直接原因是一个具体的不满：用 Logseq 记东西的人，记了一年两年之后，想读回去很难，想写成文章更难。

Logseq 适合往里写。打开今天的 Journal，想到什么记什么，大纲一层层缩进，没有负担。但重读是另一回事：想找三个月前那条读书笔记，要回到当天 Journal，一层层展开折叠的大纲，顺着你已经不记得的结构往下翻。至于成稿——库里躺着几百上千条 block，想从里面写出一篇博客或者公众号文章，只能翻找、复制、贴到别的编辑器、重新排版。从库到稿之间没有路。

妙言是一个成熟的 Markdown 编辑器，实时预览、版本历史、iCloud 同步、导出都是现成且打磨过的。在 MIT 许可下基于它做，比重写一个编辑器合理得多。青梧继承这个底盘，增量全部投在妙言和 Logseq 都没覆盖的那一段：把库读出来，把稿写出去。

## 解决什么问题

三个问题，按重要程度排：

**读不回。** 青梧把 Logseq 库渲染成正常文档的样子：TODO 显示为任务标记，`key::` 属性行正确呈现，`id::`、`collapsed::` 这类元数据不碍眼，tab 缩进的大纲展开成列表，`[[wikilink]]` 可以跳。打开一个文件夹就能读，不用在 Logseq 里考古。

**写不出。** 这是核心，也是目前最欠缺的部分。方向是让库里的 block 和页面能被选出来、组合进一篇线性文档，在青梧里编辑定稿，然后导出。这一步做通了，青梧才成立。

**不敢托付。** 让一个第三方工具打开自己记了几年的库，用户最怕它偷偷改了什么。青梧对此立了死规矩：打开 Logseq vault 绝不往库内写任何文件，冲突备份放在应用自己的目录，外部修改按最后写入生效。这条规矩写在代码里，也有测试守着，之后还要写成对用户的公开说明。

## 定位

青梧是一个本地 Markdown 文件库的阅读和成稿工具，对 Logseq 库做一等支持。

早期对外传播会说"Logseq 用户的第二界面"，因为这群人的痛点最明确、也最少有人服务。但产品本身不绑死在 Logseq 上：任何一个文件夹的 Markdown，都应该能在青梧里读和写。Logseq 官方在往数据库版走，如果哪天文件格式的 Logseq 式微，青梧仍然成立——这是刻意留的退路。

三件事明确不做：

- 不抢记录入口。青梧假设用户已经有自己的记录工具和习惯，服务的是记录之后的事。
- 不做协作。单机、本地优先，不碰多人场景。
- 近期不做 AI。如果以后做整理类的 AI 功能，必须是可选的、本地或自带 key 的，不能破坏本地优先的前提。

## 给谁用

主力是**用 Logseq 记东西、同时需要把内容写成文章发出去的人**：写博客的、写公众号的、写 newsletter 的、写技术文档的。他们库里已经有足够多的积累，缺的只是读和写的后半程，为工具付费的意愿也最明确。

其次是用文件夹管理 Markdown 的写作者，比如 Hugo、Jekyll 用户。兼容 frontmatter 之后这部分是自然延伸，不需要专门投入。

妙言的老用户自然迁移，不专门争取，但他们是早期口碑的来源。

不做从零开始记笔记的新手，那是 Obsidian 和 Apple Notes 的市场。

## 功能怎么做

原则只有一条：研发资源投给成稿链路，其余继承来的功能只维护、不投入。

**主线，持续投入：**

- Logseq 阅读渲染。已做完任务标记、属性行、缩进大纲、wikilink 解析。接下来补 block 引用和嵌入的只读渲染、namespace 页面导航、大库打开速度。
- 成稿模式。产品里目前不存在的东西，也是差异化所在：从库里选 block 或页面，组合进一篇文档，编辑定稿，导出。
- 导出和发布。PDF、HTML 已经有了，缺的是中文写作者真正的出口：公众号样式（代码块不折行、图片不丢）、博客样式。目标是导出就能发，不用排第二遍。

**只维护，不投入：**

实时预览、PPT 模式、版本历史、iCloud 同步、图床上传。这些是妙言的遗产，也是基本体验，坏了要修，但不再加新东西。PPT 模式保留——它是妙言用户愿意迁过来的原因之一。

**明确不接：**

WebDAV 之类的双向同步、插件系统、协作、看板和数据库视图。最后一项特别说明：那是 Logseq 自己该做的事，青梧不做。

Logseq 兼容只做到"读"这一层。query、动态 block 这类需要执行的东西不做，做了就是把 Logseq 重写一遍。要单独写一份公开的兼容性说明，告诉用户支持什么、不支持什么、为什么不支持，免得 issue 区被期望淹没。

## 版本计划

**V1.x：把"读"做扎实，发出真正的 1.0。** 收敛 Logseq 渲染兼容，修阅读的高频痛点（大库启动、wikilink 跳转、双链面板），发兼容性说明。发布先小范围给 Logseq 中文社区试用，收集问题，再公开宣传。直发和 App Store 两个渠道分别发，互不替代。

**V2.x：成稿模式。** 从库到稿的那一步，加上公众号和博客两个导出样式。这个版本做不出来，青梧就还只是一个功能很多的 fork，所以它的优先级高于其他一切。

**V3.x 以后再说。** AI 整理之类的想法先放着，等成稿闭环验证成立再动。

## 怎么判断做成了

盯一个数：每周有多少人通过青梧把内容发出去了（导出或直接发布）。所有功能都要能回答"它对成稿有没有帮助"，答不上来的往后排。

另一个更直接的检验：我自己每天在用。Logseq 库挂在青梧里读，每周用它写一篇东西发出去。我用不下去的地方，就是下一个版本的需求。

最后，信任相关的规矩优先级高于一切功能：不写用户的库、数据留在本地、没有埋点统计。任何需求和这些冲突时，需求让步。

---

定位要变的时候，先改这份文档，再动代码。
