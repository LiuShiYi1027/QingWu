# 青梧路线图

给开发用的执行文件。定位和功能取舍的理由看 `PRODUCT.md`，这份文件只说：现在做到哪了、接下来按什么顺序做什么、什么不接。

## 现状

V1.2 已合入，"读"这条线的主体已经完成：

- Logseq vault 只读安全打开（不动库内文件，冲突备份在应用目录）
- 任务标记、`key::` 属性行、隐藏属性、tab 缩进大纲的渲染
- wikilink 目标解析（别名、大小写、namespace 文件名映射）
- `((uuid))` block 引用在预览、导出、复制 HTML 里内联展开
- Today's Journal 快捷入口（cmd+shift+D）

macOS 和 iOS 的渲染镜像、测试、AGENTS.md 同步都保持得不错，继续保持这个习惯。

## V1.x：收口，然后发 1.0

"读"这条线到 block refs 为止基本饱和，不再加深。V1.x 剩下四件事，按顺序：

**1. 修阅读的高频痛点，不修低频的。**

判断标准是我自己日常用着卡不卡。目前重点看三个：

- 大库（几百篇以上）打开和切换笔记的速度
- wikilink 点击跳转的成功率和反馈（跳不过去的时候用户要知道为什么）
- 双链面板的数据对不对、刷新及不及时

修到什么程度算完：我拿自己的真实库连续用一周，没有"读不下去"的时刻。不要顺手做渲染兼容的新语法支持，那是另一类工作，见下面的停手线。

**2. 写公开的《Logseq 兼容性说明》。**

告诉用户青梧读 Logseq 库支持什么、不支持什么、为什么不支持。不支持清单至少包括：query、动态 block、embed 的完整语义。这是给用户的承诺，也是给自己挡 issue 的墙。写完放进 README 或官网。

**3. 发 1.0。**

直发和 App Store 两个渠道分别走各自的发布流程，互不替代。先发直发版，小范围给 Logseq 中文社区试用一到两周，收集问题修一轮，再公宣和提 App Store。

**4. iOS 的跨笔记 block 解析可以推迟。**

现在 iOS 只解析当前笔记内的 block ref，跨笔记保持原文。这个缺口不影响 1.0，记在这里，V2 再补。

### 停手线（重要）

V1.x 之后，"Logseq 渲染兼容"类需求默认不接。包括但不限于：更全的属性类型、namespace 层级导航、query 的某种"简单支持"、embed 的完整实现。这类需求每一个看起来都不大，合在一起就是重写 Logseq。有用户提，就指向《兼容性说明》；确实高频的，记录到这份文件末尾的"搁置清单"，攒到 V2 中期统一复议一次，不单独开版本。

## V2.x：成稿模式

这是产品成立与否的关键版本，1.0 发布后立即启动，优先级高于一切。

**核心功能：成稿。**

用户在写一篇准备发布的文档时，需要把库里已有的 block 和页面组装进来。MVP 只做一件事：

- 在笔记里写 `[[页面]]` 或 `((block 引用))`，导出（含复制 HTML）时递归展开成完整正文。预览保持现在的 chip 样式不展开，导出才展开——预览是编辑场景，导出是成稿场景，两个行为不一样，不要做成同一个。
- 底层复用现有的 `WikilinkIndex` block index 和 `blockResolver`，展开逻辑加在导出路径上，不要动编辑器和预览。
- 展开要有循环保护（A 引用 B、B 引用 A 时报提示并中止展开，不要死循环或静默截断）。

做到什么程度算完：我从自己的 Logseq 库里挑素材，在青梧里写一篇真实的文章，导出的东西不需要手工再补内容。

**配套：两个导出样式。**

- 公众号样式：代码块不折行、图片不丢、标题层级和引用样式过得去
- 博客样式：干净的通用排版

目标是导出即可发，不用排第二遍。样式做不出来时去看维护者指定的参考站点，不要凭空发挥。

**V2 中期补：** iOS 跨笔记 block index、搁置清单里复议通过的需求。

## V3.x 及以后

AI 整理消化之类的想法先不排期。等成稿闭环被真实使用验证（指标见下），再开讨论。届时任何 AI 功能必须是可选的、本地或自带 key 的，不动本地优先的前提。

## 判断标准

一个数：每周通过青梧导出或发布出去的成稿数。所有功能排期都要能回答"这对成稿有没有帮助"。

我自己是第 0 号用户：库挂在青梧里读，每周用它写一篇发出去。我用不下去的地方就是需求，优先级最高。

## 不变的红线

- 绝不写入 Logseq vault 内部文件；冲突备份在应用目录，不在库内
- 本地优先，不加埋点、不加遥测
- macOS / iOS 渲染逻辑保持镜像，改动同 commit 双端同步，AGENTS.md 同步更新
- 新需求先对照 `PRODUCT.md` 的定位；对不上的，默认答案是不做

## 搁置清单

V1.x 之后收到、但按停手线暂缓的需求记在这里，V2 中期复议：

- （空）
