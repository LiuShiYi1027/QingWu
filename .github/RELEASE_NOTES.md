# V1.0.1 Seedling 🌱

1. 新增专注布局（cmd+shift+K）：单栏内容模式，界面退后，启动自动回到今日笔记
2. Logseq vault 内禁用全文自动排版与附件清理：库拥有格式主权，青梧只做访客
3. 修复专注布局与 Format 的快捷键冲突（原 cmd+shift+L）
4. 完善 vault 识别：存储根目录存在 logseq/config.edn 即按 Logseq 库对待

---

1. New Focus Layout (cmd+shift+K): single content column, chrome recedes, launches into Today's Journal
2. Whole-file auto-format and attachment cleanup are now disabled inside Logseq vaults — the graph owns its formatting, QingWu is only a guest
3. Fixed the Focus Layout vs Format shortcut conflict (was cmd+shift+L)
4. Vault detection: a logseq/config.edn at the storage root marks it as a Logseq graph
